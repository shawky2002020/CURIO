/**
 * Passport.js configuration stub.
 * In production, this sets up Google OAuth strategy for social login.
 */
export const configurePassport = (): void => {
  // TODO: Configure passport strategies here if OAuth uses passport.js:
  // passport.use(new GoogleStrategy({ ... }));
};
export default configurePassport;
